# 📦 Інструкція по розгортанню на Render.com

## Крок 1: Завантаження на GitHub

### 1.1 Створи репозиторій на GitHub

1. Зайди на [github.com](https://github.com)
2. Натисни "+" в правому верхньому куті → "New repository"
3. Назва: `taina-poshta-bot` (або будь-яка інша)
4. Зроби репозиторій **Public** або **Private** (на твій вибір)
5. **НЕ СТАВЬ** галочки "Add a README file"
6. Натисни "Create repository"

### 1.2 Завантаж код

Відкрий термінал/командний рядок у папці з проектом:

```bash
# Ініціалізуй Git
git init

# Додай всі файли
git add .

# Зроби перший коміт
git commit -m "Initial commit: Taina Poshta Bot"

# Додай remote (замість YOUR_USERNAME вставте своє ім'я)
git remote add origin https://github.com/YOUR_USERNAME/taina-poshta-bot.git

# Відправ на GitHub
git branch -M main
git push -u origin main
```

## Крок 2: Створення бази даних на Render

1. Зайди на [render.com](https://render.com)
2. Залогінься через GitHub
3. Натисни **"New +"** в правому верхньому куті
4. Вибери **"PostgreSQL"**
5. Заповни форму:
   - **Name**: `taina-poshta-db`
   - **Database**: залиш `taina-poshta-db`
   - **User**: залиш як є
   - **Region**: `Frankfurt (EU Central)` (найближче до України)
   - **PostgreSQL Version**: залиш останню
   - **Datadog API Key**: залиш порожнім
   - **Instance Type**: вибери **"Free"**
6. Натисни **"Create Database"**
7. Зачекай 1-2 хвилини поки база створюється
8. Після створення знайди **"Internal Database URL"** і **СКОПІЮЙ** його (виглядає як `postgresql://user:pass@host/db`)

## Крок 3: Розгортання бота на Render

### 3.1 Створення Web Service

1. Знову натисни **"New +"** → **"Web Service"**
2. Підключи GitHub репозиторій:
   - Натисни **"Connect account"** для GitHub (якщо ще не підключений)
   - Знайди свій репозиторій `taina-poshta-bot`
   - Натисни **"Connect"**

### 3.2 Налаштування сервісу

Заповни форму:

- **Name**: `taina-poshta-bot`
- **Region**: `Frankfurt (EU Central)` (той самий, що і база даних)
- **Branch**: `main`
- **Root Directory**: залиш порожнім
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python bot.py`
- **Instance Type**: **"Free"**

### 3.3 Додавання змінних оточення

У розділі **"Environment Variables"** натисни **"Add Environment Variable"** і додай:

**Перша змінна:**
- Key: `BOT_TOKEN`
- Value: `8058408359:AAFn-y55IC2PIOTikmPLPME_NQQgQ0jKEFs`

**Друга змінна:**
- Key: `DATABASE_URL`
- Value: (вставте Internal Database URL з кроку 2.8)

### 3.4 Деплой

1. Натисни **"Create Web Service"**
2. Render почне розгортання (це займе 2-5 хвилин)
3. Слідкуй за логами в секції "Logs"
4. Коли побачиш "Starting Taina Poshta Bot..." - бот працює! ✅

## Крок 4: Перевірка

1. Відкрий Telegram
2. Знайди свого бота `@TainaPoshtaBot`
3. Надішли `/start`
4. Якщо бот відповідає - все працює! 🎉

## 🔧 Що робити якщо щось не працює

### Бот не відповідає

1. Зайди на Render → твій Web Service
2. Перевір "Logs" на помилки
3. Переконайся, що обидві змінні оточення додані правильно
4. Спробуй "Manual Deploy" → "Deploy latest commit"

### Помилка з базою даних

1. Перевір, що DATABASE_URL правильний (з PostgreSQL, а не з SQLite)
2. Переконайся, що база даних на Render активна і працює
3. Перевір логи бази даних на Render

### Змінна BOT_TOKEN неправильна

1. Перевір токен у @BotFather
2. Оновити змінну оточення на Render
3. Зроби Manual Deploy

## 📊 Моніторинг

### Логи бота
- Render → твій Web Service → вкладка "Logs"
- Тут бачиш всі дії бота в реальному часі

### Статус сервісу
- Render показує статус: "Live" 🟢 (працює) або "Failed" 🔴 (помилка)
- На безкоштовному плані сервіс засинає після 15 хвилин неактивності
- Коли хтось напише боту - він автоматично прокинеться (перше повідомлення може бути з затримкою)

## 🔄 Оновлення коду

Коли потрібно змінити код бота:

```bash
# Зроби зміни в коді
# Потім:
git add .
git commit -m "Опис змін"
git push
```

Render автоматично побачить зміни і передеплоїть бота!

## 💰 Ліміти безкоштовного плану Render

- ✅ 750 годин роботи на місяць (достатньо для одного бота 24/7)
- ✅ PostgreSQL база до 1 GB (вистачить на тисячі користувачів)
- ⚠️ Сервіс засинає після 15 хвилин неактивності
- ⚠️ Перший запит після сну займає 30-60 секунд

Для молодіжної групи до 100 осіб цього більш ніж достатньо! 🎉

## 🆘 Потрібна допомога?

Якщо щось не виходить:
1. Перевір логи на Render
2. Переконайся, що всі кроки виконані правильно
3. Напиши мені (@Astahov) - допоможу!

---

Успішного деплою! 🚀
